Flask==2.2.5
# lexitrail#232: Werkzeug is Flask's transitive dep and Flask 2.2.x does not cap it, so an
# UNPINNED build resolves whatever is newest at build time. 3.1.8 is what the LIVE pod runs
# today (verified: kubectl exec -n lexitrail <backend-pod> -- pip list), so this pin is a
# no-op for production and closes the forward risk: 3.1 already removed werkzeug.__version__,
# and the next release lands in the next image build with nothing asserting compatibility.
# NOT a fix for the test client -- that break is real and separate (see #232).
Werkzeug==3.1.8
Flask-SQLAlchemy==3.0.2
pymysql==1.0.3
python-dotenv==0.19.2
SQLAlchemy==2.0.21
google-cloud-storage==2.10.0
cryptography==3.4.8
urllib3==1.26.15
flask-cors==3.0.10
# lexitrail#266: the API sent NOTHING compressed. Measured against production 2026-08-29:
# GET /wordsets/6/words returned 688,345 B, byte-identical under an explicit
# `Accept-Encoding: gzip,deflate,br` and with no content-encoding header -- so this was not a
# negotiation subtlety, nothing was compressing anywhere (the GCLB in front does not either).
# gzip -9 of those same bytes is 180,730 B: 3.8x, on the largest wordset, for a config change.
#
# Flask-Compress rather than a hand-rolled `after_request`, deliberately: the easy thing to get
# wrong by hand is `Vary: Accept-Encoding`, and omitting it poisons every shared cache in front
# of the API by letting one client's encoding be served to another. Pinned, like everything else
# here, per #232's reasoning about unpinned transitive resolution.
Flask-Compress==1.14

# Testing and coverage
pytest==6.2.5
coverage==6.3

# Google Authentication
google-auth==2.21.0

# Additional dependencies for hint generation
google-cloud-aiplatform==1.69.0 # For Vertex AI services
google-genai==1.28.0 # gemini-2.5-flash + gemini-2.5-flash-image (lexitrail#47; old vertexai imagen/gemini-1.5 models retired)
Pillow==10.2.0  # Updated from 9.2.0 for Python 3.13 compatibility
cachetools==5.3.3  # For in-memory TTL caching