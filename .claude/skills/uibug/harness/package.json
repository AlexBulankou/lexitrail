{
  "name": "uibug-harness",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright harness for the /uibug skill. Test-only: never a dependency of ui/.",
  "scripts": {
    "sweep": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
